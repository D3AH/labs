<Input class="m-2 p-2 w-full rounded bg-gray-100 font-bold" @value={{@query}} @key-down={{perform this.search}}/>

{{!-- <PowerSelect
  @searchEnabled={{true}}
  @options={{this.result}}
  @disabled={{false}}
  @search={{perform this.search}}
  @onChange={{action (mut this.query name)}} as |name|>
  {{name.title}}
</PowerSelect> --}}